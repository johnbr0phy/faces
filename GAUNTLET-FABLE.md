# Paste into Fable (or any agent). Do not add a second paragraph.

Open `/Users/johnbrophy/faces`. Read `GAUNTLET.md` and follow it exactly.

Start at step 0 (`scripts/round.sh before`). Use a separate subagent for the critic, the builder, and the A/B. You orchestrate; you do not grade the pixels. After each verdict run `keep` or `revert`, then the next round. Do not ask which piece to do. Stop after eight rounds, or when a critic would rather look at our plate than Mannay’s.

The last failed move was growing the nose out of the silhouette (new skull fields, always-bump). That made the heads weird and has been reverted. Do not try it again. Change how the marks look.
